{
  "name": "wishcart-root",
  "version": "1.0.0",
  "description": "WishCart root configuration for monorepo deployment",
  "scripts": {
    "build": "npm install --prefix server",
    "start": "node server/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
